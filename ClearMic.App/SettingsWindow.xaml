<Window x:Class="ClearMic.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Paramètres ClearMic"
        Width="400" Height="300"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Périphériques" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel>
                <Label Content="Micro d'entrée :" />
                <ComboBox x:Name="InputDeviceCombo" />
                <Label Content="Sortie virtuelle :" />
                <ComboBox x:Name="OutputDeviceCombo" />
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Filtre" Grid.Row="1" Margin="0,0,0,10">
            <StackPanel>
                <CheckBox x:Name="NoiseFilterCheck" Content="Suppression de bruit" IsChecked="True" />
                <CheckBox x:Name="AecCheck" Content="Annulation d'écho (AEC)" Margin="0,6,0,0" />
                <TextBlock Text="Nécessite un redémarrage du pipeline" FontSize="10"
                           Foreground="#a6adc8" Margin="20,2,0,0" />
            </StackPanel>
        </GroupBox>

        <Button Grid.Row="2" Width="100" Height="30"
                HorizontalAlignment="Right"
                Click="OnSaveClick">
            Enregistrer
        </Button>
    </Grid>
</Window>
