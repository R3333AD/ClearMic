<Window x:Class="ClearMic.App.ProfileSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profils ClearMic"
        Width="500" Height="420"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Les profils s'activent automatiquement quand la fenêtre correspondante est au premier plan."
                   TextWrapping="Wrap" FontSize="11" Foreground="#a6adc8" Margin="0,0,0,10" />

        <ListBox x:Name="ProfileList" Grid.Row="1"
                 DisplayMemberPath="Name" SelectionChanged="OnProfileSelected" />

        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <Border BorderBrush="#45475a" BorderThickness="1" CornerRadius="4"
                    Padding="10" Margin="0,0,0,10" Background="#1e1e2e">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Nom :" />
                    <TextBox x:Name="ProfileNameBox" Grid.Row="0" Grid.Column="1" Margin="5,0" />

                    <Label Grid.Row="1" Grid.Column="0" Content="Processus (virgule) :" />
                    <TextBox x:Name="ProcessBox" Grid.Row="1" Grid.Column="1" Margin="5,0"
                             ToolTip="Noms de processus/ fenêtre pour l'auto-switch" />

                    <CheckBox x:Name="NoiseFilterCheck" Grid.Row="2" Grid.Column="1"
                              Content="Suppression de bruit" Margin="5,4" IsChecked="True" />
                    <CheckBox x:Name="AecCheck" Grid.Row="3" Grid.Column="1"
                              Content="Annulation d'écho (AEC)" Margin="5,4" />
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AddButton" Content="Ajouter" Width="80" Height="28"
                        Margin="0,0,6,0" Click="OnAddClick" />
                <Button x:Name="DeleteButton" Content="Supprimer" Width="80" Height="28"
                        Margin="0,0,6,0" Click="OnDeleteClick" IsEnabled="False" />
                <Button Content="Fermer" Width="80" Height="28" Click="OnCloseClick" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
